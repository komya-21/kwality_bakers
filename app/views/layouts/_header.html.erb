 <div class="brand">
                <a href="index.html" class="logo">
                    <i class="icon-layers"></i>
                    <span>Gauri</span>enterprise</a>
            </div>
            <!--logo end-->
            <ul class="nav navbar-nav navbar-left">
                <li class="toggle-navigation toggle-left">
                    <button class="sidebar-toggle" id="toggle-left">
                        <i class="fa fa-bars"></i>
                    </button>
                </li>
               
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown profile hidden-xs">
                    <a href="javascript:void(0);" class="dropdown-toggle" data-toggle="dropdown">
                        <span class="meta">
                            <span class="avatar">
                                
                            </span>
     <% if current_user.role == "SuperAdmin" || current_user.role == "Vendor" %>
    <span class="text"><%= current_user.role %></span>
                        <% elsif current_user.role == "Employee" %>
                        <span class="text"><%= current_user.employee.name%></span>
                        <% else %>
                        <span class="text"><%= current_user.location.name%></span>
                        <% end %>
                        <span class="caret"></span>
                        </span>
                    </a>
                    <ul class="dropdown-menu animated fadeInRight" role="menu">
                       
                       
                        <!--  <li>
                            <a href="/users/password/edit">
                                <span class="icon"><i class="fa fa-user"></i>
                                </span>My Account</a>
                        </li> -->
                        
                        
                       <!--  <li>
                            <a href="javascript:void(0);">
                                <span class="icon"><i class="fa fa-cog"></i>
                                </span>Settings</a>
                        </li> -->
                        
                        <li>
                            <a href="/users/sign_out" data-method='delete'>
                                <span class="icon"><i class="fa fa-sign-out"></i>
                                </span>Logout</a>
                        </li>
                    </ul>
                </li>
                <li class="toggle-fullscreen hidden-xs">
                    <button type="button" class="btn btn-default expand" id="toggle-fullscreen">
                        <i class="fa fa-expand"></i>
                    </button>
                </li>
                
           </ul>
