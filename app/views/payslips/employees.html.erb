<section id="main-content" class="animated fadeInUp">
                      <div class="row">
                    <div class="col-md-12">
                        <div class=" panel panel-info">
                          <div class="panel-heading">
                            <h3 class="panel-title">Select Employee To Create Payslip</h3>
                            <div class="actions pull-right">
                             <i class="fa fa-expand"></i>
                                <i class="fa fa-chevron-down"></i>
                                <i class="fa fa-times"></i>
                            </div>
                          </div>
                          <div class="panel-body">
<%#= link_to 'New Employee Payroll', new_employee_payroll_path ,class: "btn btn-primary"%>
                      
<%#= link_to 'New Color', new_color_path,
            :class => 'btn btn-primary' %>
<table id="example" class="table table-striped table-bordered" cellspacing="0" width="100%">
  <thead>
    <tr>
      <th>Employee Name</th>
    </tr>
  </thead>
  <tbody>
    <% @employees.each do |e| %>
    <tr>
      <td><%= link_to e.name ,new_payslip_path(employee_id: e.id) %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>
</div>
</div>
</div>
</div>
</section>