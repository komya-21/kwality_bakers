<section id="main-content" class="animated fadeInUp">
                      <div class="row">
                    <div class="col-md-12">
                        <div class=" panel panel-info">
                          <div class="panel-heading">
                            <h3 class="panel-title">Deductions</h3>
                            <div class="actions pull-right">
                             <i class="fa fa-expand"></i>
                                <i class="fa fa-chevron-down"></i>
                                <i class="fa fa-times"></i>
                            </div>
                          </div>
                          <div class="panel-body">
<%#= link_to 'New Employee Payroll', new_employee_payroll_path ,class: "btn btn-primary"%>                        
<%#= link_to 'New Color', new_color_path,
            :class => 'btn btn-primary' %>
<table id="example" class="table table-striped table-bordered" cellspacing="0" width="50%">
  <thead>
    <tr>
      <th>Name</th>
      <th>Percentage</th>
    </tr>
  </thead>
  <tbody>
    <% @deductions.each do |e| %>
    <tr>
      <td><%= e.employee_payroll.category_name %></td>
      <td><%= e.employee_payroll.percentage %>
      </td>
    </tr>
  </tbody>

<% end %>
</table>
<%= link_to 'Back', employee_payrolls_path,class: "btn btn-success" %>

</div>
</div>
</div>
</div>
</section>